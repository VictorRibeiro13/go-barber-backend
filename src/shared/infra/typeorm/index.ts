import { createConnection } from 'typeorm';

/**
 *  procurar no projeto o ermconfig.json e faz a conexão.
 *  Tambem é fazer a conexão passando os parametros para esse método
 * */

createConnection();
